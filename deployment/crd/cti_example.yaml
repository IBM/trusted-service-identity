apiVersion: "trusted.identity/v1"
kind: ClusterTI
metadata:
  name: cluster-policy
info:
  cluster-name: mycluster
  region: us-south
policy:
  - image: "trustedseriviceidentity/"
    identity: id-trustedseriviceidentity
  - image: "ibmcom/"
    identity: id-ibm
